name: Production Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deployment:
    runs-on: ubuntu-latest

    environment:
      name: production
      url: https://quickdrop-csbd.onrender.com/

    steps:
      - run: echo "QuickDrop is deployed on Render"
